# Remove the rootCA
- name: "### --- Playbook to remove the rootCA --- ###"
  hosts: pi
  # connection: local
  # become_user: pi
  become: false
  gather_facts: true
  vars:
    are_you_sure: false
  # Beginn TASKs
  tasks:
    # Remove the RootCA
    - name: Remove the rootCA-Folder-Structure
      ansible.builtin.import_role:
        name: rootca-with-openssl
        tasks_from: remove-rootca.yaml
